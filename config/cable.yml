development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: <%=Rails.application.credentials.redis[:REDIS_URL] { "redis://localhost:6379/1" }  %>
  channel_prefix: neighborhood-aid_production
